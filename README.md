# Todo-list
I built a Todo List application using HTML, CSS, and JavaScript where users can add, delete, and mark tasks as completed. Tasks are stored in an array of objects and rendered dynamically using DOM manipulation. I used array methods like map, filter, and some to manage tasks efficiently, along with event listeners for user interactions.
